# UI Scaling Checklist

- repeated field layout extracted to shared wrapper
- section modules have typed interfaces
- status and confidence indicators are shared primitives
- async states standardized (idle/loading/error/success)
- page shell supports section ordering and feature flags
